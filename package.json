{
  "name": "malaysian-tax-skill",
  "version": "1.0.0",
  "description": "AI skill for Malaysian tax computation — all entity types under ITA 1967",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js --all",
    "build:claude": "node scripts/build.js --provider claude-code",
    "build:cursor": "node scripts/build.js --provider cursor",
    "build:windsurf": "node scripts/build.js --provider windsurf",
    "build:gemini": "node scripts/build.js --provider gemini",
    "clean": "rm -rf dist"
  },
  "keywords": ["malaysian-tax", "tax-computation", "ai-skill", "ita-1967", "lhdn"],
  "license": "MIT"
}
